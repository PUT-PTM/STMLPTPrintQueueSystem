{% extends 'base.html.twig' %}

{% block body %}
    <h2>New category</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('app_admin_categories_show') }}">Back to categories list</a>
{% endblock %}